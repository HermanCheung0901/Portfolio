import "../../../../public/styles/book-note/addPage.css";

export const metadata = {
  title: "Add Book Note",
};

export default function RootLayout({
  children,
}) {
  return <div>
        {children}
    </div>;
}